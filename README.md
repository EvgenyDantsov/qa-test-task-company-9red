## QA Test Task Ц Playwright + Java

јвтоматизированные UI-тесты написаны с использованием:

- Java 11
- Maven
- JUnit 4
- Playwright for Java

“ест провер€ет корректность заголовка главной страницы сайта Playwright.

---
## “ребовани€

ѕеред запуском убедитесь, что установлены:

- Java 11
- Maven 3.9
- ƒоступ в интернет (дл€ скачивани€ браузеров Playwright)
---
## ÷ель тестировани€

“ест провер€ет корректность заголовка главной страници и выполн€етс€ в браузерах Chromium и Firefox.

---
## ѕроверить версии

ѕровер€ем соответстви€ версий Java и Maven
```bash
java -version
mvn -version
```
---
## ”становка зависимостей

ѕерейдите в корень проекта (где находитс€ pom.xml) и выполните:
```bash
mvn clean install
```
---
## ”становка браузеров Playwright (выполн€етс€ один раз после клонировани€ проекта):
¬ыполните команду:
```bash
mvn exec:java -Dexec.mainClass=com.microsoft.playwright.CLI -Dexec.args="install"
```
ѕосле выполнени€ браузеры будут скачаны автоматически.
≈сли используетс€ прокси, необходимо установить переменные окружени€:
```cmd
set HTTP_PROXY=http://proxy:port
set HTTPS_PROXY=http://proxy:port
```
ѕосле этого повторно запустите команду установки браузеров.

---
## «апуск тестов:
ѕо умолчанию тесты запускаютс€ в headless режиме (без открыти€ окна браузера)
```bash
mvn test
```
---
## «апуск тестов c открытием браузера:

```bash
mvn test -Dheadless=false
```